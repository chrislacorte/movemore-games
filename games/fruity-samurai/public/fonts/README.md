# Fonts

| File | Usage |
|------|--------|
| `AssassinNinja.otf` | Title & game branding (`.font-title`) |
| `fruity-ui.woff2` | Optional UI font (`.font-ui`) — falls back to Nunito |

Source file: `Fonts/aAssassinNinja.otf` in project root.
